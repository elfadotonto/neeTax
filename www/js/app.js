'use strict';

var modules = [
        'ngRoute',
        'neeTax.config',
        'neeTax.constants',
        'neeTax.taxer',
        'neeTax.filters'
    ];
// Declare app level module which depends on views, and components
angular.module('neeTax', modules);
